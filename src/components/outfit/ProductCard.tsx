"use client";
import { memo } from "react";
import Image from "next/image";
import { aspectClass, type Product } from "@/lib/outfit/products";
export const ProductCard = memo(function ProductCard({ product, priority = false }: { product: Product; priority?: boolean }) {
  return (<a className="group block" data-product="true" data-cursor="text" href={`/product/${product.slug}`}><div className="relative overflow-hidden" data-inview="true"><Image data-image="front" alt={product.name} draggable={false} width={1920} height={1920} priority={priority} loading={priority?"eager":"lazy"} unoptimized className={aspectClass(product.aspect)} src={product.frontImage} sizes="(min-width: 768px) 25vw, 100vw" /><Image data-image="back" alt={product.name} draggable={false} width={1920} height={1920} loading="lazy" unoptimized className="h-full w-full object-cover aspect-square ease-[cubic-bezier(0.87,0,0.13,1)] absolute inset-0 transition-all duration-500 scale-[1.2] group-hover:scale-[1] [clip-path:polygon(0%_0%,_0%_0%,_0%_100%,_0%_100%)] group-hover:[clip-path:polygon(0%_0%,_100%_0%,_100%_100%,_0%_100%)] filter-[brightness(400%)_contrast(150%)] group-hover:filter-[brightness(100%)_contrast(100%)]" src={product.backImage} sizes="(min-width: 768px) 25vw, 100vw" /><div className="absolute inset-0 h-full w-full bg-[#d2cac3] dark:bg-neutral-800" /></div><div className="mt-3 flex flex-col justify-between gap-2 text-lg leading-5 md:flex-row"><p>{product.name}</p><p><span>{product.price}</span></p></div><div className="mt-[6px] flex items-center gap-1 text-[11px] uppercase"><div className="h-2 w-2 rounded-full bg-current" /><span>{product.category}</span></div></a>);
});
