{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test": "pnpm -r --filter \"./artifacts/**\" --if-present run test",
    "test:e2e": "pnpm --filter @workspace/artcovr run test:e2e",
    "verify:ci": "pnpm run typecheck && pnpm run test",
    "verify:release": "pnpm run verify:ci && pnpm run test:e2e && pnpm --filter @workspace/scripts run verify:runtime-contract",
    "verify:live-release": "pnpm --filter @workspace/scripts run verify:live-release",
    "verify:database": "bash scripts/db/verify-database.sh",
    "db:migrate": "pnpm --filter @workspace/db run migrate"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1",
    "stripe": "^22.6.0",
    "stripe-replit-sync": "^1.0.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
